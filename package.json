{
  "name": "image-hosting-service",
  "version": "1.0.0",
  "description": "GitHub based image hosting service",
  "main": "index.js",
  "scripts": {
    "dev": "wrangler pages dev public --port 4566",
    "build": "npm install && npm run build:functions",
    "build:functions": "wrangler pages functions build --outdir=dist"
  },
  "dependencies": {
    "@octokit/rest": "^20.0.0"
  },
  "devDependencies": {
    "wrangler": "^3.99.0"
  }
}